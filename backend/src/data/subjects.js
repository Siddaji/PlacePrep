const subjects = [
  {
    id: "os",
    title: "Operating Systems",
    short: "OS",
    color: "violet",
    description: "Processes, threads, scheduling, memory management, deadlocks, and synchronization.",
    topics: [
      {
        id: "os-1",
        subject: "OS",
        title: "Process vs Thread",
        difficulty: "Easy",
        frequency: "Very High",
        summary: "A process is an independent program in execution with its own memory. A thread is a lightweight unit within a process that shares memory with other threads.",
        mustKnow: [
          "A process is an independent program in execution with its own memory space",
          "A thread is a lightweight unit of execution within a process",
          "Threads within the same process share heap, code, data, and files",
          "Each thread has its own stack and registers",
          "Process creation is slower and more expensive than thread creation",
          "A crash in one process does not affect other processes",
          "A crash in one thread can crash the entire process",
          "Processes communicate via IPC — pipes, sockets, shared memory",
          "Threads communicate directly via shared memory — faster but needs synchronization",
          "Context switch between threads is faster than between processes",
        ],
        explanation: "A process is a program in execution. When you open Chrome, the OS creates a process with its own memory space — code, stack, heap, and data segments. Processes are isolated from each other for safety. A thread is a unit of execution within a process. One process can spawn multiple threads that run concurrently. Threads share the same memory space, making communication between them fast — but also risky, since one thread can corrupt data that another thread is using. This is why synchronization mechanisms like mutex and semaphores exist.",
        analogy: {
          emoji: "🍽️",
          title: "Restaurant vs Chefs",
          text: "A process is like an entire restaurant — has its own building, kitchen, staff. Two restaurants are completely independent. A thread is like a chef within one restaurant — multiple chefs share the same kitchen and ingredients. Chefs coordinate fast (shared kitchen) but must not grab the same knife simultaneously (synchronization needed).",
        },
        interviewQA: [
          {
            q: "What is the difference between a process and a thread?",
            a: "A process is an independent program in execution with its own memory space. A thread is a lightweight unit of execution within a process. The key difference is memory — processes have isolated memory, threads share memory within the same process. Thread creation is faster, context switching is cheaper, but a crash in one thread can affect the whole process.",
          },
          {
            q: "What resources are shared between threads?",
            a: "Threads within the same process share code segment, data segment, heap memory, file descriptors, and signal handlers. Each thread has its own stack, registers, and program counter.",
          },
          {
            q: "Why does Chrome use separate processes for each tab?",
            a: "Chrome uses one process per tab for isolation and security. If one tab crashes or is compromised by malicious code, it cannot access memory of other tabs or crash the entire browser. The isolation comes at a cost — more memory usage — but the stability and security trade-off is worth it.",
          },
          {
            q: "What is a context switch?",
            a: "A context switch is when the CPU stops executing one process or thread and starts executing another. The OS saves the current state (registers, program counter, stack pointer) of the running process and loads the saved state of the next one. Thread context switches are faster than process context switches because threads share memory — less state to save and restore.",
          },
        ],
        selfCheck: [
          "What is the difference between a process and a thread?",
          "What memory do threads share within a process?",
          "Why is thread context switching faster than process context switching?",
          "Why does Chrome use separate processes per tab?",
          "What happens to other threads if one thread crashes?",
        ],
      },
      {
        id: "os-2",
        subject: "OS",
        title: "CPU Scheduling Algorithms",
        difficulty: "Medium",
        frequency: "High",
        summary: "The OS decides which process runs on the CPU at any given time using scheduling algorithms. Each algorithm optimizes for different goals.",
        mustKnow: [
          "FCFS — First Come First Served — simple, non-preemptive, convoy effect problem",
          "SJF — Shortest Job First — optimal average waiting time, needs future knowledge",
          "Round Robin — fixed time quantum, preemptive, good for time-sharing",
          "Priority Scheduling — higher priority runs first, starvation problem",
          "Preemptive vs Non-preemptive — can OS interrupt a running process?",
          "Convoy effect — short jobs wait behind one long job in FCFS",
          "Starvation — low priority process never gets CPU time",
          "Aging — gradually increase priority of waiting processes to prevent starvation",
          "Turnaround time = completion time - arrival time",
          "Waiting time = turnaround time - burst time",
        ],
        explanation: "The CPU scheduler decides which process in the ready queue gets to run next. Different algorithms optimize for different goals — minimizing waiting time, maximizing throughput, or ensuring fairness. FCFS is the simplest — whoever arrives first runs first. SJF minimizes average waiting time but needs to know burst time in advance. Round Robin gives each process a fixed time slice (quantum) before preempting — good for interactive systems. Priority scheduling runs higher priority processes first but risks starvation of low-priority processes.",
        analogy: {
          emoji: "🏥",
          title: "Hospital Emergency Room",
          text: "FCFS is a regular queue — first patient in, first treated. SJF is treating the quickest case first — maximizes throughput but someone with a long complex case waits forever. Round Robin is giving each patient exactly 10 minutes then moving to the next — fair but nobody gets continuous care. Priority scheduling is treating critical cases first — correct for emergencies but minor cases might wait all day (starvation).",
        },
        interviewQA: [
          {
            q: "What is the convoy effect in FCFS scheduling?",
            a: "The convoy effect occurs when a long CPU-bound process holds the CPU while many short processes wait in the queue. Like a slow truck on a highway causing a convoy of fast cars behind it. This increases average waiting time significantly.",
          },
          {
            q: "Why is SJF optimal for average waiting time?",
            a: "SJF (Shortest Job First) minimizes average waiting time because shorter jobs complete quickly and free the CPU for others. Mathematically, processing shorter jobs first reduces the total cumulative waiting time across all processes. The problem is it requires knowing burst time in advance — which is generally not possible in practice.",
          },
          {
            q: "What is the difference between preemptive and non-preemptive scheduling?",
            a: "In non-preemptive scheduling, once a process gets the CPU it runs until it completes or blocks. In preemptive scheduling, the OS can interrupt a running process and switch to another — for example when a higher priority process arrives or the time quantum expires in Round Robin.",
          },
          {
            q: "What is starvation and how is it solved?",
            a: "Starvation is when a process waits indefinitely because higher priority processes keep arriving. It is solved by aging — gradually increasing the priority of a waiting process over time so it eventually gets CPU time.",
          },
        ],
        selfCheck: [
          "Explain FCFS and its main drawback.",
          "Why is SJF optimal but impractical?",
          "How does Round Robin work and what is the quantum?",
          "What is starvation and how is aging used to fix it?",
          "Difference between preemptive and non-preemptive scheduling?",
        ],
      },
      {
        id: "os-3",
        subject: "OS",
        title: "Deadlocks",
        difficulty: "Medium",
        frequency: "Very High",
        summary: "A deadlock occurs when two or more processes are waiting for resources held by each other, causing all of them to wait forever.",
        mustKnow: [
          "Deadlock — processes waiting forever for resources held by each other",
          "Four necessary conditions — Mutual Exclusion, Hold and Wait, No Preemption, Circular Wait",
          "ALL four conditions must hold simultaneously for deadlock to occur",
          "Prevention — eliminate at least one of the four conditions",
          "Avoidance — Banker's Algorithm checks if granting a request leads to safe state",
          "Detection — allow deadlock, detect it, then recover",
          "Recovery — kill a process or preempt resources",
          "Resource Allocation Graph — circle = process, square = resource",
          "Cycle in RAG = deadlock possible (not guaranteed if resource has multiple instances)",
        ],
        explanation: "A deadlock occurs when each process in a set is waiting for a resource held by another process in the same set — creating a circular wait with no escape. Four conditions must ALL be true simultaneously for a deadlock: Mutual Exclusion (only one process can use a resource), Hold and Wait (process holds one resource while waiting for another), No Preemption (resources cannot be forcibly taken), and Circular Wait (circular chain of waiting). Deadlock handling strategies are Prevention (break one condition), Avoidance (Banker's Algorithm), and Detection and Recovery.",
        analogy: {
          emoji: "🚗",
          title: "Four-Way Traffic Jam",
          text: "Four cars at a four-way intersection, each car has pulled into the intersection and is waiting for the car ahead to move. Car A waits for Car B's spot. Car B waits for Car C's spot. Car C waits for Car D's spot. Car D waits for Car A's spot. Nobody can move. This is a perfect deadlock — circular wait with no preemption (nobody backs up).",
        },
        interviewQA: [
          {
            q: "What are the four necessary conditions for a deadlock?",
            a: "Mutual Exclusion — only one process can use a resource at a time. Hold and Wait — a process holds at least one resource while waiting for additional resources. No Preemption — resources cannot be forcibly taken from a process. Circular Wait — a circular chain of processes where each waits for a resource held by the next. All four must be true simultaneously for a deadlock.",
          },
          {
            q: "What is the Banker's Algorithm?",
            a: "The Banker's Algorithm is a deadlock avoidance algorithm. Before granting a resource request, it simulates the allocation and checks if the resulting state is safe — meaning there exists a sequence in which all processes can complete. If the state is safe, grant the request. If not, make the process wait. It requires knowing maximum resource needs in advance.",
          },
          {
            q: "How do you prevent deadlock?",
            a: "By eliminating at least one of the four necessary conditions. Eliminate Mutual Exclusion by making resources shareable where possible. Eliminate Hold and Wait by requiring processes to request all resources at once. Eliminate No Preemption by allowing the OS to preempt resources. Eliminate Circular Wait by imposing a total ordering on resource types and requiring processes to request resources in order.",
          },
          {
            q: "What is the difference between deadlock prevention and deadlock avoidance?",
            a: "Prevention eliminates one of the four conditions structurally — it ensures deadlock can never occur but is often overly restrictive. Avoidance allows more flexibility — it checks each resource request dynamically using algorithms like Banker's to ensure the system stays in a safe state.",
          },
        ],
        selfCheck: [
          "What are the four necessary conditions for deadlock?",
          "Draw and explain the four-way traffic jam analogy for deadlock.",
          "What is the Banker's Algorithm and when is it used?",
          "What is the difference between deadlock prevention and avoidance?",
          "How can circular wait be eliminated?",
        ],
      },
      {
        id: "os-4",
        subject: "OS",
        title: "Memory Management",
        difficulty: "Medium",
        frequency: "High",
        summary: "How the OS manages RAM — allocating memory to processes, virtual memory, paging, and handling memory that exceeds physical RAM.",
        mustKnow: [
          "Virtual memory — gives each process illusion of having entire address space",
          "Paging — divide memory into fixed-size pages, eliminates external fragmentation",
          "Page table — maps virtual page numbers to physical frame numbers",
          "TLB — Translation Lookaside Buffer — cache for page table lookups",
          "Page fault — accessing a page not in RAM, OS loads it from disk",
          "Thrashing — excessive page faults, system spends more time swapping than executing",
          "Internal fragmentation — wasted space within allocated block",
          "External fragmentation — free space scattered in non-contiguous chunks",
          "Segmentation — divide memory into variable-size segments by logical meaning",
          "Demand paging — pages loaded into RAM only when needed",
        ],
        explanation: "Memory management allows multiple processes to share RAM safely and efficiently. Virtual memory gives each process the illusion of having the entire address space — the OS maps virtual addresses to physical addresses using a page table. Paging divides both virtual and physical memory into fixed-size blocks (pages and frames). When a process accesses a page not in RAM, a page fault occurs — the OS loads the page from disk (swap space). If too many page faults occur, the system thrashes — spending more time swapping pages than actually executing code.",
        analogy: {
          emoji: "📚",
          title: "Library with Limited Desks",
          text: "The library has thousands of books (disk storage) but only 10 desk slots (RAM). Students (processes) think they have access to all books. When a student needs a book not on their desk, the librarian fetches it from the shelf (page fault — load from disk). If all desks are full, one book must go back to the shelf first. If students keep needing books that are on the shelf (too many page faults), the librarian spends all day fetching books and nobody actually reads — that is thrashing.",
        },
        interviewQA: [
          {
            q: "What is virtual memory and why is it useful?",
            a: "Virtual memory is a memory management technique that gives each process the illusion of having access to a large contiguous memory space, regardless of actual physical RAM. It is useful because it allows processes to use more memory than physically available by storing inactive pages on disk, enables memory isolation between processes, and simplifies programming since each process thinks it has the entire address space.",
          },
          {
            q: "What is a page fault?",
            a: "A page fault occurs when a process accesses a virtual memory page that is not currently loaded in physical RAM. The OS interrupts the process, finds the page on disk, loads it into a free frame in RAM (evicting another page if necessary), updates the page table, and resumes the process. Page faults are normal — too many page faults (thrashing) indicate a problem.",
          },
          {
            q: "What is thrashing?",
            a: "Thrashing occurs when a process spends more time handling page faults and swapping pages than actually executing. It happens when a process does not have enough frames allocated to hold its working set — the set of pages it actively uses. The system constantly swaps pages in and out, CPU utilization drops, and the system appears frozen.",
          },
          {
            q: "What is the difference between paging and segmentation?",
            a: "Paging divides memory into fixed-size pages — simple, eliminates external fragmentation, but causes internal fragmentation. Segmentation divides memory into variable-size segments based on logical meaning (code segment, stack, heap) — no internal fragmentation, more intuitive, but suffers from external fragmentation. Modern systems use a combination — segmented paging.",
          },
        ],
        selfCheck: [
          "What is virtual memory and what problem does it solve?",
          "What happens when a page fault occurs?",
          "What is thrashing and what causes it?",
          "Difference between paging and segmentation?",
          "What is the TLB and why does it exist?",
        ],
      },
      {
        id: "os-5",
        subject: "OS",
        title: "Semaphores & Mutex",
        difficulty: "Medium",
        frequency: "High",
        summary: "Synchronization tools that prevent race conditions when multiple threads access shared resources concurrently.",
        mustKnow: [
          "Race condition — outcome depends on order of thread execution",
          "Critical section — code that accesses shared resource, only one thread at a time",
          "Mutex — mutual exclusion lock, only one thread can hold it at a time",
          "Semaphore — integer counter, can allow multiple threads (counting semaphore)",
          "Binary semaphore — value 0 or 1, similar to mutex",
          "Counting semaphore — value 0 to N, controls access to N identical resources",
          "wait() / P() — decrements semaphore, blocks if value is 0",
          "signal() / V() — increments semaphore, wakes a blocked thread",
          "Deadlock can occur with improper use of semaphores",
          "Producer-Consumer problem — classic semaphore use case",
        ],
        explanation: "When multiple threads share resources, race conditions can occur — the final result depends on which thread runs first, making the program non-deterministic. Synchronization tools prevent this. A mutex is a lock — only one thread can hold it at a time. Thread A acquires mutex, does its work, releases mutex. Thread B waits until Thread A releases. A semaphore is more general — it is an integer counter. wait() decrements it and blocks if zero. signal() increments it and wakes a waiting thread. Binary semaphores work like mutexes. Counting semaphores control access to a pool of N identical resources.",
        analogy: {
          emoji: "🚻",
          title: "Bathroom Key",
          text: "A single bathroom with one key is a mutex. Only one person can have the key. Others wait outside. When done, the key is returned and the next person takes it. A semaphore with value 3 is three bathrooms with three keys. Up to three people can use bathrooms simultaneously. The fourth person waits until someone returns a key. wait() is taking a key. signal() is returning a key.",
        },
        interviewQA: [
          {
            q: "What is a race condition?",
            a: "A race condition occurs when two or more threads access shared data concurrently and the final result depends on the order in which they execute. For example, if Thread A and Thread B both read a counter value of 5 and both increment it, both write 6 back — the counter should be 7 but is 6. Race conditions cause non-deterministic bugs that are hard to reproduce.",
          },
          {
            q: "What is the difference between a mutex and a semaphore?",
            a: "A mutex is a locking mechanism — only the thread that acquired the mutex can release it. It provides mutual exclusion for one resource. A semaphore is a signaling mechanism — any thread can call signal(). A binary semaphore (0 or 1) functions similarly to a mutex. A counting semaphore controls access to multiple instances of a resource. The key difference: mutex has ownership (only acquirer can release), semaphore does not.",
          },
          {
            q: "Explain the Producer-Consumer problem.",
            a: "The Producer-Consumer problem involves a shared buffer. Producers add items to the buffer. Consumers remove items. Problems: producer should not add when buffer is full, consumer should not remove when buffer is empty, and both should not access the buffer simultaneously. Solved with two counting semaphores — empty (initialized to buffer size) and full (initialized to 0) — plus a mutex for mutual exclusion on the buffer.",
          },
          {
            q: "What is a critical section?",
            a: "A critical section is a segment of code that accesses a shared resource and must not be executed by more than one thread simultaneously. Every critical section problem solution must satisfy three conditions — mutual exclusion (only one thread in critical section at a time), progress (if no thread is in critical section, one waiting thread must be allowed in), and bounded waiting (a thread must not wait forever to enter).",
          },
        ],
        selfCheck: [
          "What is a race condition? Give a simple example.",
          "What is the difference between a mutex and a semaphore?",
          "What is a critical section?",
          "Explain the Producer-Consumer problem and how to solve it.",
          "What is the difference between binary and counting semaphores?",
        ],
      },
    ],
  },
  {
    id: "dbms",
    title: "Database Management Systems",
    short: "DBMS",
    color: "blue",
    description: "ACID properties, normalization, indexing, transactions, joins, and SQL queries.",
    topics: [],
  },
  {
    id: "cn",
    title: "Computer Networks",
    short: "CN",
    color: "emerald",
    description: "OSI model, TCP/IP, HTTP vs HTTPS, DNS, TCP vs UDP, and subnetting.",
    topics: [],
  },
  {
    id: "oop",
    title: "Object Oriented Programming",
    short: "OOP",
    color: "rose",
    description: "Four pillars, SOLID principles, design patterns, and common interview problems.",
    topics: [],
  },
  {
    id: "dbms",
    title: "Database Management Systems",
    short: "DBMS",
    color: "blue",
    description: "ACID properties, normalization, indexing, transactions, joins, and SQL queries.",
    topics: [
      {
        id: "dbms-1",
        subject: "DBMS",
        title: "ACID Properties",
        difficulty: "Easy",
        frequency: "Very High",
        summary: "ACID is a set of four properties that guarantee database transactions are processed reliably — Atomicity, Consistency, Isolation, Durability.",
        mustKnow: [
          "ACID stands for Atomicity, Consistency, Isolation, Durability",
          "Atomicity — transaction either fully completes or fully rolls back, no partial updates",
          "Consistency — database moves from one valid state to another, all rules are maintained",
          "Isolation — concurrent transactions do not interfere with each other",
          "Durability — once committed, data survives crashes and restarts",
          "ACID is guaranteed by relational databases like PostgreSQL and MySQL",
          "NoSQL databases typically follow BASE instead of ACID",
          "Transactions group multiple operations into one atomic unit",
          "COMMIT saves the transaction, ROLLBACK undoes it",
          "Isolation levels — Read Uncommitted, Read Committed, Repeatable Read, Serializable",
        ],
        explanation: "ACID properties ensure that database transactions are reliable and correct even in the presence of errors, crashes, or concurrent access. Atomicity means a transaction is all-or-nothing — if a bank transfer debits one account but the credit fails, the debit is rolled back automatically. Consistency ensures the database never violates its defined rules — a balance cannot go negative if a constraint prevents it. Isolation ensures two transactions running simultaneously behave as if they ran sequentially. Durability means committed data is permanently saved even if the server crashes immediately after.",
        analogy: {
          emoji: "💸",
          title: "UPI Bank Transfer",
          text: "You transfer ₹500 via UPI. Atomicity: either your balance goes down AND the recipient's goes up — or neither happens. ₹500 cannot vanish in the middle. Consistency: your balance cannot go below zero (constraint maintained). Isolation: two people transferring money simultaneously don't interfere — each transfer sees a consistent state. Durability: once the bank confirms the transfer, it is permanent even if their server crashes one second later.",
        },
        interviewQA: [
          {
            q: "What does ACID stand for and what does each property mean?",
            a: "Atomicity — a transaction is all or nothing, no partial updates. Consistency — the database moves from one valid state to another, all integrity constraints are maintained. Isolation — concurrent transactions execute as if they were sequential, intermediate states are not visible to others. Durability — once a transaction is committed, it persists permanently even in the event of a system crash.",
          },
          {
            q: "What is the difference between ACID and BASE?",
            a: "ACID (used by SQL databases) prioritizes consistency and reliability — every transaction must fully satisfy all four properties. BASE (used by NoSQL databases) prioritizes availability and performance — Basically Available, Soft state, Eventually consistent. ACID is suitable for financial and medical data where correctness is critical. BASE is suitable for social feeds, analytics, and content where slight inconsistency is acceptable.",
          },
          {
            q: "What are isolation levels in a database?",
            a: "Isolation levels control how much one transaction can see of another's uncommitted changes. Read Uncommitted — can read dirty (uncommitted) data, fastest but least safe. Read Committed — only reads committed data, prevents dirty reads. Repeatable Read — same query returns same result within a transaction. Serializable — strictest level, transactions execute as if completely sequential, slowest but most consistent.",
          },
          {
            q: "What is a dirty read?",
            a: "A dirty read occurs when a transaction reads data that has been modified by another transaction that has not yet committed. If that other transaction rolls back, the first transaction has read data that never officially existed. This is prevented by isolation levels Read Committed and above.",
          },
        ],
        selfCheck: [
          "What does ACID stand for? Explain each property.",
          "Give a real-world example that illustrates Atomicity.",
          "What is the difference between ACID and BASE?",
          "What are the four isolation levels and what problem does each solve?",
          "What is a dirty read?",
        ],
      },
      {
        id: "dbms-2",
        subject: "DBMS",
        title: "Normalization",
        difficulty: "Medium",
        frequency: "Very High",
        summary: "Normalization is the process of organizing a database to reduce data redundancy and improve data integrity by decomposing tables into smaller, well-structured ones.",
        mustKnow: [
          "Normalization reduces data redundancy and update anomalies",
          "1NF — atomic values only, no repeating groups, each column has a single value",
          "2NF — must be in 1NF, no partial dependency (non-key attribute depends on full primary key)",
          "3NF — must be in 2NF, no transitive dependency (non-key attribute depends on another non-key attribute)",
          "BCNF — stricter version of 3NF, every determinant must be a candidate key",
          "Insertion anomaly — cannot insert data without adding unrelated data",
          "Update anomaly — changing one value requires updating multiple rows",
          "Deletion anomaly — deleting one row accidentally deletes unrelated data",
          "Denormalization — intentionally adding redundancy for performance (used in read-heavy systems)",
          "Functional dependency — X → Y means knowing X determines Y",
        ],
        explanation: "Normalization is a systematic process of organizing database tables to eliminate redundancy. Without normalization, updating one piece of data requires changing it in multiple places — miss one and data becomes inconsistent. The normal forms (1NF through BCNF) are progressive rules. 1NF ensures atomic values. 2NF eliminates partial dependencies where a non-key column depends only on part of a composite primary key. 3NF eliminates transitive dependencies where a non-key column depends on another non-key column. BCNF handles edge cases 3NF misses.",
        analogy: {
          emoji: "📦",
          title: "Warehouse Organization",
          text: "Imagine a warehouse where every box has the supplier's full address written on it. If the supplier moves, you must update every single box. That is an update anomaly. Normalization separates the supplier info into a separate 'Suppliers' record and just references it from each box. Now when the supplier moves, update one record — all boxes automatically reflect the change.",
        },
        interviewQA: [
          {
            q: "What is normalization and why is it important?",
            a: "Normalization is the process of organizing database tables to reduce data redundancy and eliminate anomalies. It is important because unnormalized databases suffer from update anomalies (changing data in one place leaves inconsistencies elsewhere), insertion anomalies (cannot insert data without adding unrelated data), and deletion anomalies (deleting one row accidentally removes other useful data).",
          },
          {
            q: "Explain 1NF, 2NF, and 3NF with an example.",
            a: "1NF requires atomic values — no column should contain multiple values. Example: storing 'Math, Science' in a subjects column violates 1NF. 2NF requires no partial dependency — in a table with composite primary key (StudentID, CourseID), if TeacherName depends only on CourseID and not on StudentID, that is a partial dependency violating 2NF. 3NF requires no transitive dependency — if ZipCode determines City, and StudentID determines ZipCode, then City transitively depends on StudentID through ZipCode, violating 3NF.",
          },
          {
            q: "What is denormalization and when would you use it?",
            a: "Denormalization intentionally introduces redundancy into a database by combining tables or duplicating data to improve read performance. It is used when the system is read-heavy and joins are becoming a performance bottleneck. For example, storing a user's name directly in the orders table instead of joining the users table on every order query. The trade-off is higher storage and more complex update logic.",
          },
          {
            q: "What is the difference between 3NF and BCNF?",
            a: "3NF allows a non-prime attribute to determine another non-prime attribute as long as both are part of a candidate key. BCNF is stricter — every determinant must be a candidate key, no exceptions. BCNF eliminates anomalies that 3NF can miss. The practical difference appears in tables with overlapping candidate keys. Most real-world databases target 3NF — BCNF decomposition can sometimes cause loss of functional dependencies.",
          },
        ],
        selfCheck: [
          "What are the three types of anomalies that normalization prevents?",
          "Explain 1NF, 2NF, and 3NF in simple terms.",
          "What is a partial dependency and which normal form removes it?",
          "What is a transitive dependency and which normal form removes it?",
          "When would you intentionally denormalize a database?",
        ],
      },
      {
        id: "dbms-3",
        subject: "DBMS",
        title: "Indexing",
        difficulty: "Medium",
        frequency: "High",
        summary: "An index is a data structure that speeds up data retrieval by allowing the database to find rows without scanning the entire table.",
        mustKnow: [
          "Index is like a book index — points directly to the data location without full scan",
          "Without index — full table scan O(n), with index — O(log n) for B-tree",
          "Primary index — created automatically on primary key",
          "Secondary index — created on non-primary key columns for faster queries",
          "Clustered index — physically reorders table data to match index order, only one per table",
          "Non-clustered index — separate structure pointing to actual rows, multiple allowed",
          "B-tree index — most common, good for range queries and equality checks",
          "Hash index — O(1) lookup but only for equality, no range queries",
          "Indexes speed up SELECT but slow down INSERT, UPDATE, DELETE",
          "Too many indexes hurt write performance — index only columns used in WHERE, JOIN, ORDER BY",
        ],
        explanation: "An index is a separate data structure maintained by the database that maps column values to row locations. Without an index, finding a student by email requires scanning every row — O(n). With a B-tree index on email, the database traverses the tree in O(log n). A clustered index determines the physical order of data in the table — like a dictionary where words are stored alphabetically. A non-clustered index is a separate structure pointing to rows — like a book index at the back. Each table can have only one clustered index but multiple non-clustered indexes.",
        analogy: {
          emoji: "📖",
          title: "Book Index vs Reading Every Page",
          text: "Finding 'Binary Search' in a 500-page textbook without an index — you read every page until you find it (full table scan, O(n)). With the index at the back, you look up 'Binary Search', see page 247, flip directly there (O(log n)). The index takes extra space and must be updated when content changes — same trade-offs as database indexes.",
        },
        interviewQA: [
          {
            q: "What is the difference between clustered and non-clustered indexes?",
            a: "A clustered index determines the physical storage order of rows in the table — the table data itself is sorted by the clustered index key. There can only be one clustered index per table (usually the primary key). A non-clustered index is a separate structure with pointers to the actual rows — the data rows are not reordered. A table can have multiple non-clustered indexes. Clustered index is faster for range queries; non-clustered requires an extra lookup to fetch the row.",
          },
          {
            q: "Why do indexes slow down write operations?",
            a: "Every INSERT, UPDATE, or DELETE must also update all indexes on the table to keep them consistent. If a table has five indexes and you insert a row, the database must update all five index structures. This overhead makes writes slower. This is why you should not index every column — only index columns that are frequently used in WHERE clauses, JOIN conditions, or ORDER BY.",
          },
          {
            q: "What is a composite index and when is it useful?",
            a: "A composite index is an index on multiple columns. For example, an index on (last_name, first_name) allows fast queries filtering by last_name alone or by both last_name and first_name together. It does not help queries filtering only by first_name — the leftmost column rule applies. Composite indexes are useful when queries frequently filter on the same combination of columns.",
          },
          {
            q: "What is the difference between a B-tree and Hash index?",
            a: "A B-tree index stores data in a sorted tree structure. It supports equality queries (WHERE email = 'x'), range queries (WHERE age BETWEEN 20 AND 30), and ORDER BY operations. Time complexity O(log n). A Hash index uses a hash function to map values to locations — O(1) for equality queries but cannot support range queries or sorting. B-tree is the default for most databases; Hash index is used only when all queries are exact equality checks.",
          },
        ],
        selfCheck: [
          "What is an index and what problem does it solve?",
          "Difference between clustered and non-clustered index?",
          "Why do indexes slow down write operations?",
          "When would you use a Hash index instead of a B-tree index?",
          "What is the leftmost column rule for composite indexes?",
        ],
      },
      {
        id: "dbms-4",
        subject: "DBMS",
        title: "SQL Joins",
        difficulty: "Medium",
        frequency: "Very High",
        summary: "Joins combine rows from two or more tables based on a related column. Understanding all join types and when to use each is essential for every placement interview.",
        mustKnow: [
          "INNER JOIN — returns only rows with matching values in both tables",
          "LEFT JOIN — returns all rows from left table, matching rows from right, NULL if no match",
          "RIGHT JOIN — returns all rows from right table, matching rows from left, NULL if no match",
          "FULL OUTER JOIN — returns all rows from both tables, NULL where no match",
          "CROSS JOIN — returns Cartesian product, every row from left combined with every row from right",
          "SELF JOIN — joining a table with itself, used for hierarchical data",
          "ON clause specifies the join condition",
          "JOIN performance depends on indexes on join columns",
          "Multiple joins can be chained in one query",
          "NATURAL JOIN — automatically joins on columns with same name (avoid in production)",
        ],
        explanation: "Joins are the most powerful feature of relational databases — they let you combine data from multiple tables in a single query. INNER JOIN returns only rows where the join condition matches in both tables — if a student has no solved problems, they would not appear. LEFT JOIN keeps all students even if they have no solved problems — missing values become NULL. RIGHT JOIN is the mirror of LEFT JOIN. FULL OUTER JOIN combines both — all rows from both tables, NULL where no match exists. Joins work best when the join columns are indexed.",
        analogy: {
          emoji: "🔗",
          title: "Student + Results Lists",
          text: "INNER JOIN: Match students with their results — only students who appeared in the exam show up. LEFT JOIN: Show all students — those who did not appear in the exam get a NULL result. RIGHT JOIN: Show all results — if a result has no matching student (data entry error), it still shows. FULL OUTER JOIN: Show everything — all students and all results, NULLs where no match exists.",
        },
        interviewQA: [
          {
            q: "What is the difference between INNER JOIN and LEFT JOIN?",
            a: "INNER JOIN returns only the rows where the join condition matches in both tables. If a row in the left table has no matching row in the right table, it is excluded entirely. LEFT JOIN returns all rows from the left table regardless of whether there is a match in the right table. Where there is no match, columns from the right table are filled with NULL. Use LEFT JOIN when you need all records from the primary table even if related data does not exist.",
          },
          {
            q: "Write a query to find all students who have not solved any problem.",
            a: "SELECT s.name FROM students s LEFT JOIN solved_problems sp ON s.id = sp.student_id WHERE sp.student_id IS NULL. This uses a LEFT JOIN to keep all students, then filters where the right table's key is NULL — meaning no matching solved problem was found for that student.",
          },
          {
            q: "What is a SELF JOIN and when is it used?",
            a: "A SELF JOIN joins a table with itself. It is used for hierarchical or recursive data. Example: an employees table where each employee has a manager_id that references another employee's id in the same table. To find each employee and their manager name: SELECT e.name as employee, m.name as manager FROM employees e LEFT JOIN employees m ON e.manager_id = m.id.",
          },
          {
            q: "What is the difference between WHERE and HAVING?",
            a: "WHERE filters rows before grouping — it cannot use aggregate functions. HAVING filters groups after GROUP BY — it can use aggregate functions. Example: WHERE salary > 50000 filters individual rows before aggregation. HAVING COUNT(*) > 5 filters groups after counting. Always use WHERE for row-level filtering and HAVING for group-level filtering.",
          },
        ],
        selfCheck: [
          "What is the difference between INNER JOIN and LEFT JOIN?",
          "Write a query to find students who have not solved any problem.",
          "What is a SELF JOIN and give a real example.",
          "What is the difference between WHERE and HAVING?",
          "What does CROSS JOIN return and when would you use it?",
        ],
      },
      {
        id: "dbms-5",
        subject: "DBMS",
        title: "Transactions & Concurrency",
        difficulty: "Medium",
        frequency: "High",
        summary: "A transaction is a sequence of operations treated as a single logical unit. Concurrency control ensures multiple transactions execute correctly without conflicts.",
        mustKnow: [
          "Transaction — sequence of operations that must all succeed or all fail together",
          "BEGIN, COMMIT, ROLLBACK — start, save, undo a transaction",
          "Concurrency problems — dirty read, non-repeatable read, phantom read",
          "Dirty read — reading uncommitted data from another transaction",
          "Non-repeatable read — same query returns different results within one transaction",
          "Phantom read — new rows appear in a repeated query within one transaction",
          "Locking — pessimistic concurrency control, blocks other transactions",
          "MVCC — Multi-Version Concurrency Control, optimistic, each transaction sees a snapshot",
          "Deadlock in transactions — two transactions waiting for each other's locks",
          "Savepoint — partial rollback within a transaction",
        ],
        explanation: "A transaction groups multiple SQL operations so they either all succeed (COMMIT) or all fail (ROLLBACK). Without transactions, a server crash mid-operation leaves the database in an inconsistent state. Concurrency problems arise when multiple transactions run simultaneously. A dirty read happens when Transaction A reads data modified but not yet committed by Transaction B — if B rolls back, A has read phantom data. MVCC (used by PostgreSQL) solves concurrency without blocking — each transaction gets a consistent snapshot of the database at its start time, allowing reads and writes to proceed simultaneously without locking.",
        analogy: {
          emoji: "🏦",
          title: "Bank Transfer with Undo",
          text: "BEGIN: start the transfer. Deduct ₹500 from Account A. Add ₹500 to Account B. If both succeed — COMMIT: the transfer is permanent. If adding to Account B fails — ROLLBACK: the deduction from Account A is automatically reversed. The bank never loses your money in the middle of the operation. SAVEPOINT is like a partial checkpoint — you can roll back to that point without undoing everything.",
        },
        interviewQA: [
          {
            q: "What are the three concurrency problems in databases?",
            a: "Dirty Read — Transaction A reads data modified by Transaction B which has not committed yet. If B rolls back, A has read invalid data. Non-Repeatable Read — Transaction A reads a row, Transaction B updates it and commits, Transaction A reads the same row again and gets a different value. Phantom Read — Transaction A queries rows matching a condition, Transaction B inserts a new matching row and commits, Transaction A re-runs the query and sees a new row that did not exist before.",
          },
          {
            q: "What is MVCC and how does it improve concurrency?",
            a: "MVCC (Multi-Version Concurrency Control) allows multiple transactions to read and write concurrently without blocking each other. Instead of locking rows, the database maintains multiple versions of each row. Each transaction sees a consistent snapshot of the database as it was at the transaction's start time. Readers never block writers and writers never block readers. PostgreSQL uses MVCC by default. This improves throughput significantly compared to traditional locking.",
          },
          {
            q: "What is a deadlock in the context of database transactions?",
            a: "A deadlock occurs when two transactions are each waiting for a lock held by the other. Transaction A locks row 1 and waits for row 2. Transaction B locks row 2 and waits for row 1. Neither can proceed. Databases detect deadlocks automatically and resolve them by aborting one transaction (the victim) and rolling it back, allowing the other to proceed. The aborted transaction can then be retried.",
          },
          {
            q: "What is the difference between optimistic and pessimistic locking?",
            a: "Pessimistic locking assumes conflicts will happen — it locks data when read, preventing others from modifying it until the transaction completes. Safe but reduces concurrency. Optimistic locking assumes conflicts are rare — it does not lock data on read but checks at commit time if the data was modified by another transaction. If modified, it rolls back and retries. Better performance when conflicts are infrequent, worse when conflicts are common.",
          },
        ],
        selfCheck: [
          "What is a transaction and what are the three SQL commands that control it?",
          "Explain dirty read, non-repeatable read, and phantom read.",
          "What is MVCC and how does it differ from traditional locking?",
          "How do databases handle deadlocks?",
          "What is the difference between optimistic and pessimistic locking?",
        ],
      },
    ],
  },
];

export default subjects;